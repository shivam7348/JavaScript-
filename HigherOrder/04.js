const myObject = {
    js: 'javascript',
    cpp: 'c++',
    java: 'java',
    swift: 'swift'
}

for (const key in myObject) {
//    console.log(`${key} for shortcut  for ${myObject[key]}`);
   
}

const programing = ['js', 'cpp', 'java', 'swift']
for (const key in programing) {

    // console.log(`${key} for shortcut  for ${programing[key]}`);
    
        
    }

// const map = new Map()
// map.set("IN", "India")
// map.set("US", "United state of America")
// map.set("JP", "Japan")
// map.set("fr", "france")
// map.set("jan", "Jaunpur")
 
// for (const key in map) {
//     console.log(key);
    
//     }


// for eac